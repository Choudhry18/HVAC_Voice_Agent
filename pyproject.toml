[project]
name = "livekit-hvac-agent"
version = "0.1.0"
description = "A basic LiveKit HVAC voice agent"
readme = "README.md"
requires-python = ">=3.11,<3.15"
dependencies = [
    "livekit-agents>=1.6,<2",
    "python-dotenv>=1.1,<2",
]

[tool.uv]
package = false
